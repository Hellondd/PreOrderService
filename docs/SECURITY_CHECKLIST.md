# SECURITY_CHECKLIST.md

| Проверка | Статус | Доказательство | Что исправлено |
|----------|--------|----------------|----------------|
| .env не загружен в Git | выполнено | скриншот 01 | `.env` удалён из индекса (`git rm --cached`), добавлен в `.gitignore` |
| .env.example без реальных секретов | выполнено | скриншот 02 | Использован `.env.production.example` (переименован в `.env.example`), все значения заменены на `change_me` |
| Ручной поиск секретов выполнен | выполнено | скриншот 03 | Найденные секреты (реальный токен в `.env`) удалены. Файл `modules/BOT_TOKEN.py` удалён. |
| Зависимости проверены | выполнено | скриншот 04 | Запущен `pip-audit`, обнаружено 11 уязвимостей. Выполнено обновление пакетов до актуальных версий (см. `requirements.txt`). |
| Проверены роли | выполнено | скриншот 05 | Клиент не может зайти в `/admin` – возвращается 403. Менеджер имеет доступ. |
| Проверен доступ к чужим данным | выполнено | скриншот 06 | В клиентской панели выводятся только заказы текущего пользователя (фильтр по `user_id`). Попытка подставить чужой ID в URL не приводит к утечке. |
| CORS/hosts/debug проверены | выполнено | скриншот 07 | Заголовки ответа не содержат `Access-Control-Allow-Origin: *`. В production `debug=False` (при запуске через gunicorn). |
| Backup создан | выполнено | скриншот 08 | Создан `scripts/backup.bat`, бэкап `warehouse.db` сохранён в `backups/backup_20260609_*.db`. |
| Restore проверен | выполнено | скриншот 09 | Выполнено восстановление из бэкапа, приложение работает, данные на месте. |
| Открытые порты проверены | выполнено | скриншот 10 | Выполнен `netstat -ano`. Открыт только порт 5000 (или 10000 на Render). Лишних портов нет. |
| Логи без критических ошибок | выполнено | скриншот 11 | В логах после выполнения сценариев нет `Traceback`, `ERROR 500`. Только информационные сообщения. |
| Commit с этапом 5 | выполнено | скриншот 12 | Все изменения добавлены и запушены в `main`. |